# Dashboards

This folder contains a starter Grafana dashboard definition and a Prometheus alert example. The current simulator exposes Prometheus text output through the API, so these files are meant as a thin observability scaffold rather than a full production deployment.

