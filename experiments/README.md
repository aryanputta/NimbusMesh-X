# Experiments

Generated artifacts land here:

- `experiments/logs/*.jsonl`: per-request decision logs
- `experiments/results/*.json`: benchmark summaries

Each config already points at deterministic output paths so runs are easy to compare and replay.

