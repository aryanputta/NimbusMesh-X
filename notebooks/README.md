# Notebooks

Use this folder for result analysis, ablation plots, and decision-log replay once benchmark outputs are generated under `experiments/results/`.

Recommended first notebooks:

- latency breakdown by policy
- cache hit ratio versus p95 latency
- fairness versus cost tradeoff
- failure window replay and policy adaptation timeline

